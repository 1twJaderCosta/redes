#include "./local.h"main(int argc, char *argv[]){  int socket_servidor,bytes_enviados, bytes_recebidos;  struct sockaddr_in servidor;  int tamanho_servidor = sizeof(servidor);  char buffer[MAX_SIZE_BUFFER]="Voce recebeu isso?";  char date[30], hour[10], desliga[15]="Encerrado", no[50], sysname[50], release[50], versao[50], machine[40], credito[100];  socket_servidor = socket(AF_INET, SOCK_DGRAM, 0);  bzero(&servidor, sizeof(servidor));  servidor.sin_family        = AF_INET;  servidor.sin_port          = htons(atoi(argv[2]));  servidor.sin_addr.s_addr = inet_addr(argv[1]);printf("\n");printf("\n");printf("\n");printf("==========================================================\n");printf("================ Servidor de Comando Remoto ==============\n");printf("==========================================================\n");while(1){printf("\n127.0.0.1:5000 @  ");gets(buffer);if(strcmp(buffer,"exit")==0){printf("Desconectado!\n");return 0;}else if(strcmp(buffer,"date")==0){bytes_enviados=sendto(socket_servidor, buffer, MAX_SIZE_BUFFER, 0, (struct sockaddr *)&servidor, sizeof(servidor));  bytes_recebidos = recvfrom(socket_servidor, date,30, 0, (struct sockaddr *)&servidor, &tamanho_servidor);  printf("\n%s\n", date);  }else if(strcmp(buffer,"time")==0){  bytes_enviados=sendto(socket_servidor, buffer, MAX_SIZE_BUFFER, 0, (struct sockaddr *)&servidor, sizeof(servidor));  bytes_recebidos = recvfrom(socket_servidor, hour,10, 0, (struct sockaddr *)&servidor, &tamanho_servidor);  printf("\n%s\n", hour);  }else if(strcmp(buffer,"nodename")==0){  bytes_enviados=sendto(socket_servidor, buffer, MAX_SIZE_BUFFER, 0, (struct sockaddr *)&servidor, sizeof(servidor));  bytes_recebidos = recvfrom(socket_servidor, no,50, 0, (struct sockaddr *)&servidor, &tamanho_servidor);  printf("\n%s\n", no);  }else if(strcmp(buffer,"sysname")==0){  bytes_enviados=sendto(socket_servidor, buffer, MAX_SIZE_BUFFER, 0, (struct sockaddr *)&servidor, sizeof(servidor));  bytes_recebidos = recvfrom(socket_servidor, sysname,50, 0, (struct sockaddr *)&servidor, &tamanho_servidor);  printf("\n%s\n", sysname);  }else if(strcmp(buffer,"version")==0){  bytes_enviados=sendto(socket_servidor, buffer, MAX_SIZE_BUFFER, 0, (struct sockaddr *)&servidor, sizeof(servidor));  bytes_recebidos = recvfrom(socket_servidor, versao,40, 0, (struct sockaddr *)&servidor, &tamanho_servidor);  printf("\n%s\n", versao);  }else if(strcmp(buffer,"machine")==0){  bytes_enviados=sendto(socket_servidor, buffer, MAX_SIZE_BUFFER, 0, (struct sockaddr *)&servidor, sizeof(servidor));  bytes_recebidos = recvfrom(socket_servidor, machine,50, 0, (struct sockaddr *)&servidor, &tamanho_servidor);  printf("\n%s\n", machine);  }  else if(strcmp(buffer,"shutdown")==0){  bytes_enviados=sendto(socket_servidor, buffer, MAX_SIZE_BUFFER, 0, (struct sockaddr *)&servidor, sizeof(servidor));  bytes_recebidos = recvfrom(socket_servidor, desliga,15, 0, (struct sockaddr *)&servidor, &tamanho_servidor);  return 0;  }else if(strcmp(buffer,"release")==0){  bytes_enviados=sendto(socket_servidor, buffer, MAX_SIZE_BUFFER, 0, (struct sockaddr *)&servidor, sizeof(servidor));  bytes_recebidos = recvfrom(socket_servidor, release,50, 0, (struct sockaddr *)&servidor, &tamanho_servidor);  printf("\n%s\n", release);  }else if(strcmp(buffer,"help")==0){  printf("\n Comandos do servidor:\n ");  printf("\n date: Retorna a data\n ");  printf("\n time: Retorna a hora\n ");  printf("\n nodename: Retorna o nome do nó\n ");  printf("\n sysname: Retorna o nome do sistema\n ");  printf("\n release: Retorna a liberação do sistema\n ");  printf("\n version: Retorna a versão do sistema\n ");  printf("\n machine: Retorna o nome da maquina\n ");  printf("\n shutdown: Encerra o servidor\n ");  printf("\n credits: Exibe o nome completo dos participantes da equipe\n ");  printf("\n exit: Encerra o cliente\n ");  printf("\n help: Exibe um auxílio sobre os comandos existentes\n ");  }  else if(strcmp(buffer,"credits")==0){  bytes_enviados=sendto(socket_servidor, buffer, MAX_SIZE_BUFFER, 0, (struct sockaddr *)&servidor, sizeof(servidor));  bytes_recebidos = recvfrom(socket_servidor, credito,100, 0, (struct sockaddr *)&servidor, &tamanho_servidor);  printf("\n%s\n", credito);  }else{ bytes_enviados=sendto(socket_servidor, buffer, MAX_SIZE_BUFFER, 0, (struct sockaddr *)&servidor, sizeof(servidor));  bytes_recebidos = recvfrom(socket_servidor, buffer,MAX_SIZE_BUFFER, 0, (struct sockaddr *)&servidor, &tamanho_servidor);  printf("\nComando errado!\nDigite novamente!\n");  }}  close(socket_servidor);  return 0;}